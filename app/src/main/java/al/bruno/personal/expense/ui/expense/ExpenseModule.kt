package al.bruno.personal.expense.ui.expense

import dagger.Module

@Module
class ExpenseModule {
    /*@Provides
    fun expenseViewModel(categoriesRepository: CategoriesRepository): CategoriesViewModel {
        return CategoriesViewModel(categoriesRepository)
    }

    @Provides
    fun expenseViewModelProvider(categoriesViewModel: CategoriesViewModel): ViewModelProvider.Factory {
        return ViewModelProviderFactory(categoriesViewModel)
    }*/
}
