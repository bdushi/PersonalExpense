package al.bruno.personal.expense.ui.home

import dagger.Module

@Module
class HomeModule {
    /*@Provides
    fun homeViewModel(expenseChartRepository: ExpenseChartRepository, expenseDetailsRepository: ExpenseDetailsRepository, expenseMasterRepository: ExpenseMasterRepository): HomeViewModel {
        return HomeViewModel(expenseChartRepository, expenseDetailsRepository, expenseMasterRepository)
    }

    @Provides
    fun homeViewModelProvider(homeViewModel: HomeViewModel): ViewModelProvider.Factory {
        return ViewModelProviderFactory(homeViewModel)
    }*/
}